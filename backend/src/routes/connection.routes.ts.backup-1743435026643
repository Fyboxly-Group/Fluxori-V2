// TypeScript checked
import express from 'express';
import { connectionRoutes } from '../modules/connections';
import * as connectionController from '../controllers/connection.controller';

const router = express.Router();

// Delegate all routes to the module router
router.use('/', connection.controller.connectionRoutes);

export default router;