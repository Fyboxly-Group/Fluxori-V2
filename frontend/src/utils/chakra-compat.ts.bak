/**
 * Chakra UI Compatibility Layer
 * 
 * This file provides a compatibility layer for Chakra UI v3,
 * re-exporting components with proper direct imports.
 * 
 * It serves as a bridge to ensure code written for v2 can continue to work
 * with minimal changes when moving to v3.
 */

// Color mode hooks
import { useColorMode as useChakraColorMode } from '@chakra-ui/react/color-mode';
export const useColorMode = useChakraColorMode;

// Stat components
import { Stat } from '@chakra-ui/react/stat';
import { StatLabel } from '@chakra-ui/react/stat';
import { StatNumber } from '@chakra-ui/react/stat';
import { StatHelpText } from '@chakra-ui/react/stat';
import { StatArrow } from '@chakra-ui/react/stat';
export { Stat, StatLabel, StatNumber, StatHelpText, StatArrow };

// Alert Dialog components
import { AlertDialog } from '@chakra-ui/react/alert-dialog';
import { AlertDialogOverlay } from '@chakra-ui/react/alert-dialog';
import { AlertDialogContent } from '@chakra-ui/react/alert-dialog';
import { AlertDialogHeader } from '@chakra-ui/react/alert-dialog';
import { AlertDialogCloseButton } from '@chakra-ui/react/alert-dialog';
import { AlertDialogBody } from '@chakra-ui/react/alert-dialog';
import { AlertDialogFooter } from '@chakra-ui/react/alert-dialog';
export {
  AlertDialog,
  AlertDialogOverlay,
  AlertDialogContent,
  AlertDialogHeader,
  AlertDialogCloseButton,
  AlertDialogBody,
  AlertDialogFooter
};

// Toast functionality
import { useToast as useChakraToast } from '@chakra-ui/react/toast';
export const useToast = useChakraToast;
export const createToaster = useChakraToast;

// Tab components
import { TabList } from '@chakra-ui/react/tabs';
import { Tab } from '@chakra-ui/react/tabs';
import { TabPanels } from '@chakra-ui/react/tabs';
import { TabPanel } from '@chakra-ui/react/tabs';
export { TabList, Tab, TabPanels, TabPanel };

// Layout components
import { Divider } from '@chakra-ui/react/divider';
import { Grid } from '@chakra-ui/react/layout';
import { GridItem } from '@chakra-ui/react/layout';
export { Divider, Grid, GridItem };

// Popover components
import { PopoverCloseButton } from '@chakra-ui/react/popover';
export { PopoverCloseButton };

// Form components
import { FormControl } from '@chakra-ui/react/form-control';
import { FormLabel } from '@chakra-ui/react/form-control';
import { FormErrorMessage } from '@chakra-ui/react/form-control';
export { FormControl, FormLabel, FormErrorMessage };

// Alert components
import { Alert } from '@chakra-ui/react/alert';
import { AlertIcon } from '@chakra-ui/react/alert';
export { Alert, AlertIcon };

// Modal components
import { Modal } from '@chakra-ui/react/modal';
import { ModalOverlay } from '@chakra-ui/react/modal';
import { ModalContent } from '@chakra-ui/react/modal';
import { ModalHeader } from '@chakra-ui/react/modal';
import { ModalCloseButton } from '@chakra-ui/react/modal';
import { ModalBody } from '@chakra-ui/react/modal';
import { ModalFooter } from '@chakra-ui/react/modal';
export {
  Modal,
  ModalOverlay,
  ModalContent,
  ModalHeader,
  ModalCloseButton,
  ModalBody,
  ModalFooter
};

// Drawer components
import { DrawerOverlay } from '@chakra-ui/react/drawer';
import { DrawerCloseButton } from '@chakra-ui/react/drawer';
export { DrawerOverlay, DrawerCloseButton };

// Typography components
import { Code } from '@chakra-ui/react/text';
export { Code };

// Link component
import { Link } from '@chakra-ui/react/layout';
export { Link };

// Utilities for @chakra-ui/theme
export const createMultiStyleConfigHelpers = (parts: string[]) => {
  return {
    defineMultiStyleConfig: (config: any) => config,
    definePartsStyle: (config: any) => config,
  };
};

// Query and mutation enhancers (mock implementations)
export const enhanceMutationOptions = (options: any) => options;
export const enhanceQueryOptions = (options: any) => options;